const BASE = "/";
const HOME = BASE + "home";
const RECIPES = BASE + "recipes";
const RECIPE_DETAILS = BASE + "recipe/:id";
const ADD_RECIPE = BASE + "add-recipe";
const VOICE_ASSISTANT = BASE + "voice/assistant";

const AppRoutes = {
  BASE,
  HOME,
  RECIPES,
  RECIPE_DETAILS,
  ADD_RECIPE,
  VOICE_ASSISTANT,
};

export default AppRoutes;
